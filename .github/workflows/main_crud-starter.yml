## Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
## More GitHub Actions for Azure: https://github.com/Azure/actions
#
#name: BACKEND - Deploy Node.js app to Azure Web App - crud-starter
#
#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:
#
#jobs:
#
#  deploy:
#    runs-on: ubuntu-latest
#    environment:
#      name: 'Production'
#      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v2
#      - run: echo print content
#      - run: ls
#
#      - name: Set up Node.js version
#        uses: actions/setup-node@v2
#        with:
#          node-version: '18'
#
#      - name: Install application dependencies
#        working-directory: backend
#        run: npm install
#
#
#      - name: 'Deploy to Azure Web App'
#        id: deploy-to-webapp
#        uses: azure/webapps-deploy@v2
#        with:
#          app-name: 'crud-starter'
#          slot-name: 'Production'
#          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_E2CCEEE9F6B3409D91BE2C1A6F88F3A9 }}
#          package: backend



          name: Build and deploy Node.js app to Azure Web App - crud-starter-frontend

          on:
            push:
              branches:
                - main
            workflow_dispatch:

          jobs:
            build:
              runs-on: ubuntu-latest

              steps:
                - uses: actions/checkout@v2

                - name: Set up Node.js version
                  uses: actions/setup-node@v1
                  with:
                    node-version: '18.x'

                - name: npm install, build, and test
                  working-directory: frontend
                  run: |
                    npm install
                    npm run build --if-present
                    npm run test --if-present

#                - name: Upload artifact for deployment job
#                  uses: actions/upload-artifact@v2
#                  with:
#                    name: node-app
#                    path: dist

            deploy:
              runs-on: ubuntu-latest
              needs: build
              environment:
                name: 'Production'
                url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

              steps:
#                - name: Download artifact from build job
#                  uses: actions/download-artifact@v2
#                  with:
#                    name: node-app

                - name: 'Deploy to Azure Web App'
                  id: deploy-to-webapp
                  uses: azure/webapps-deploy@v2
                  with:
                    app-name: 'crud-starter-frontend'
                    slot-name: 'Production'
                    publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_E8CD53C30ECF411394CB403F80C0D213 }}
                    package: frontend
